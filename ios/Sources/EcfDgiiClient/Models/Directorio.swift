//
// Directorio.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Representa una entrada del directorio para contribuyentes electrónicos en el sistema de facturación electrónica  de la República Dominicana. Este modelo contiene las URLs de servicios que los contribuyentes electrónicos  deben proporcionar para la comunicación entre contribuyentes. */
public struct Directorio: Sendable, Codable, Hashable {

    /** Obtiene el nombre legal o razón social del contribuyente electrónico. La razón social registrada tal como aparece en el registro oficial del contribuyente. Este campo es requerido y debe coincidir con el nombre de la entidad legal del contribuyente. */
    public var nombre: String?
    /** Obtiene el Número de Registro Nacional del Contribuyente (RNC) del contribuyente electrónico. Un número de 9 dígitos que identifica de forma única al contribuyente en el sistema tributario  de la República Dominicana. Este campo es requerido y debe tener un formato de RNC válido. */
    public var rnc: String?
    /** Obtiene la URL del servicio de recepción de facturas electrónicas. La URL completa donde otros contribuyentes pueden enviar facturas electrónicas (e-CF) a este contribuyente. Debe seguir el formato estándar: https://host/ambiente/nombreservicio/fe/recepcion/api/ecf Este campo es obligatorio para todos los contribuyentes electrónicos. */
    public var urlRecepcion: String?
    /** Obtiene la URL del servicio de aprobación comercial. La URL completa donde otros contribuyentes pueden enviar aprobaciones comerciales para facturas recibidas. Debe seguir el formato estándar: https://host/ambiente/nombreservicio/fe/aprobacioncomercial/api/ecf Este campo es obligatorio para todos los contribuyentes electrónicos. */
    public var urlAceptacion: String?
    /** Obtiene la URL del servicio opcional de autenticación. La URL base del servicio de autenticación, si es implementado por el contribuyente. Debe seguir el formato estándar: https://host/ambiente/nombreservicio/fe/autenticacion/api/ Este campo es opcional pero recomendado para mayor seguridad. */
    public var urlOpcional: String?

    public init(nombre: String? = nil, rnc: String? = nil, urlRecepcion: String? = nil, urlAceptacion: String? = nil, urlOpcional: String? = nil) {
        self.nombre = nombre
        self.rnc = rnc
        self.urlRecepcion = urlRecepcion
        self.urlAceptacion = urlAceptacion
        self.urlOpcional = urlOpcional
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case nombre
        case rnc
        case urlRecepcion
        case urlAceptacion
        case urlOpcional
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(nombre, forKey: .nombre)
        try container.encodeIfPresent(rnc, forKey: .rnc)
        try container.encodeIfPresent(urlRecepcion, forKey: .urlRecepcion)
        try container.encodeIfPresent(urlAceptacion, forKey: .urlAceptacion)
        try container.encodeIfPresent(urlOpcional, forKey: .urlOpcional)
    }
}

